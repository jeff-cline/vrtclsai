# Copy to .env and fill in values. NEVER commit .env.
DATABASE_URL="postgresql://vrtcls:CHANGE_ME@localhost:5432/vrtclsai?schema=public"

# NextAuth — generate with: openssl rand -base64 32
AUTH_SECRET="CHANGE_ME"
AUTH_URL="http://localhost:3000"

# Seed admin (used by prisma/seed.ts only on initial seed)
SEED_ADMIN_EMAIL="jeff.cline@me.com"
SEED_ADMIN_PASSWORD="CHANGE_ME_BEFORE_DEPLOY"
SEED_ADMIN_NAME="Jeff Cline"

# Public site config
NEXT_PUBLIC_SITE_URL="https://vrtcls.ai"
NEXT_PUBLIC_SITE_NAME="Predictive Intelligence Exchange"
